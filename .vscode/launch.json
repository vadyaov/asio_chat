{
  "version": "0.2.0",
  "configurations": [
    {
    "name": "server Launch",
    "type": "lldb",
    "request": "launch",
    "program": "${workspaceFolder}/build/chat_client",
    "cwd": "${workspaceFolder}",
    },
    {
    "name": "client Launch",
    "type": "lldb",
    "request": "launch",
    "program": "${workspaceFolder}/build/chat_client",
    "args": ["127.0.0.1", "5555"],
    "cwd": "${workspaceFolder}",
    }
  ]
}